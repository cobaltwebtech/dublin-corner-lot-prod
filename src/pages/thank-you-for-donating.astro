---
import BaseLayout from "@layout/BaseLayout";
import Donation from "@comp/sections/DonationReceived";
import { SITE } from "@/data/siteConfig";

const pageTitle: string = `Thank You for Donating | ${SITE.title}`;
---

<BaseLayout title={pageTitle} noIndex={true}>
  <Donation />
</BaseLayout>
